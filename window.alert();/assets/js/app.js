/* En este ejemplo escribimos el código JavaScript dentro del HTML en el atributo "onlick=" que soporta la etiqueta <button> 

Se hizo esto porque sino, al cargar el sitio, se iba a ejecutar automaticamente la alerta en pantalla.

Si quieres probar que sucede puedes probar la siguiente línea. Deberás borrar los // para que se ejecute. */

// window.alert('Esta alerta está saliendo en pantalla cuando se carga el sitio.');